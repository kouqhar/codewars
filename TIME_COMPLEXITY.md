# Time Complexity Review (Updated)

Conservative worst-case estimates for each solution file.

| File | Time complexity | Rationale |
|---|---:|---|
| `03-03-2026/add_punctuation.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `03-03-2026/add_punctuation.py` | **O(1)** | Only constant-time arithmetic/branching detected. |
| `03-03-2026/business_day_count.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `03-03-2026/business_day_count.py` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `03-03-2026/flattened.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `03-03-2026/flattened.py` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `03-03-2026/sum_letters.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `03-03-2026/sum_letters.py` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `06-02-2026/descending_order.js` | **O(n log n)** | Sorting dominates asymptotic runtime. |
| `06-02-2026/ones_and_zeros.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `06-02-2026/people_in_the_bus.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `06-02-2026/vowel_count.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `07-02-2026/disemvowel_trolls.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `07-02-2026/exes_ohs.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `07-02-2026/isograms.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `07-02-2026/reverse_words.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `07-02-2026/testing_1-2-3.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `07-03-2026/perfect_cube_count.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `07-03-2026/perfect_cube_count.py` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `07-03-2026/playing_card_values.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `07-03-2026/playing_card_values.py` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `08-02-2026/alternate_capitalization.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `08-02-2026/count_digit.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `08-02-2026/find_stray_number.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `08-02-2026/sum_of_a_sequence.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `08-03-2026/smallest_gap.js` | **O(n²)** | Nested iteration or repeated linear lookup inside a loop. |
| `08-03-2026/smallest_gap.py` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `09-01-2026/ip_validation.py` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `09-01-2026/multi_table_for_number.py` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `09-01-2026/multiplication_table.py` | **O(n²)** | Nested iteration or repeated linear lookup inside a loop. |
| `09-01-2026/persistent_bugger.py` | **O(n²)** | Nested iteration or repeated linear lookup inside a loop. |
| `09-01-2026/valid_braces.py` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `09-02-2026/find_next_perfect_square.js` | **O(1)** | Only constant-time arithmetic/branching detected. |
| `10-02-2026/readable_time.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `10-03-2026/array_insertion.js` | **O(1)** | Only constant-time arithmetic/branching detected. |
| `10-03-2026/array_insertion.py` | **O(1)** | Only constant-time arithmetic/branching detected. |
| `10-03-2026/array_sum.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `10-03-2026/array_sum.py` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `10-03-2026/element_size.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `10-03-2026/element_size.py` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `10-03-2026/hsl_validator.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `10-03-2026/hsl_validator.py` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `11-01-2026/binary_addition.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `11-01-2026/bookseller.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `11-01-2026/no_zeros_for_heros.js` | **O(n²)** | Nested iteration or repeated linear lookup inside a loop. |
| `11-01-2026/sum_num_digits.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `11-01-2026/sum_num_digits.py` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `11-02-2026/digit_root_sum.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `11-02-2026/max_length_dif.js` | **O(n log n)** | Sorting dominates asymptotic runtime. |
| `11-02-2026/which_are_in.js` | **O(n log n)** | Sorting dominates asymptotic runtime. |
| `11-03-2026/word_len_counter.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `11-03-2026/word_len_counter.py` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `12-01-2026/check_the_exam.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `12-01-2026/function_squaring_an_argument.js` | **O(1)** | Only constant-time arithmetic/branching detected. |
| `12-01-2026/lucky_number.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `12-01-2026/remove_the_minimum.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `12-01-2026/sum_of_the_first_nth_terms_of_series.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `12-01-2026/whats-inbetween.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `12-03-2026/cate_new_member.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `12-03-2026/cate_new_member.py` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `12-03-2026/count_ip_address.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `12-03-2026/count_ip_address.py` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `12-03-2026/domino_chain_validator.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `12-03-2026/domino_chain_validator.py` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `12-03-2026/first_non_repeating_char.js` | **O(n²)** | Nested iteration or repeated linear lookup inside a loop. |
| `15-01-2026/anagram_detection.js` | **O(n log n)** | Sorting dominates asymptotic runtime. |
| `15-01-2026/consecutive_string.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `15-01-2026/find_the_middle_element.js` | **O(n log n)** | Sorting dominates asymptotic runtime. |
| `15-01-2026/growth_of_a_population.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `15-02-2026/figure_skating.js` | **O(n log n)** | Sorting dominates asymptotic runtime. |
| `15-02-2026/figure_skating.py` | **O(n log n)** | Sorting dominates asymptotic runtime. |
| `15-02-2026/luge.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `15-02-2026/luge.py` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `15-02-2026/skeleton.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `15-02-2026/skeleton.py` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `15-02-2026/speed_skating.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `15-02-2026/speed_skating.py` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `18-02-2026/alpine_skiing.js` | **O(1)** | Only constant-time arithmetic/branching detected. |
| `18-02-2026/alpine_skiing.py` | **O(1)** | Only constant-time arithmetic/branching detected. |
| `18-03-2026/anniversary_milestone.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `18-03-2026/anniversary_milestone.py` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `18-03-2026/captured_chess_pieces.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `18-03-2026/captured_chess_pieces.py` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `18-03-2026/evenly_divisible.js` | **O(1)** | Only constant-time arithmetic/branching detected. |
| `18-03-2026/evenly_divisible.py` | **O(1)** | Only constant-time arithmetic/branching detected. |
| `18-03-2026/largest_number.js` | **O(n log n)** | Sorting dominates asymptotic runtime. |
| `18-03-2026/largest_number.py` | **O(n log n)** | Sorting dominates asymptotic runtime. |
| `18-03-2026/secrete_message.py` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `19-01-2026/consecutive_strings.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `19-01-2026/find_the_middle_element.js` | **O(n log n)** | Sorting dominates asymptotic runtime. |
| `19-01-2026/find_the_singleton.js` | **O(n log n)** | Sorting dominates asymptotic runtime. |
| `19-01-2026/vowel_remover.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `19-02-2026/bobsled.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `19-02-2026/bobsled.py` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `19-02-2026/ice_hockey.js` | **O(n log n)** | Sorting dominates asymptotic runtime. |
| `19-02-2026/ice_hockey.py` | **O(n log n)** | Sorting dominates asymptotic runtime. |
| `19-02-2026/nordic_combined.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `19-02-2026/nordic_combined.py` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `19-02-2026/ski_mountaineering.js` | **O(1)** | Only constant-time arithmetic/branching detected. |
| `19-02-2026/ski_mountaineering.py` | **O(1)** | Only constant-time arithmetic/branching detected. |
| `21-01-2026/count_number_divisors.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `21-01-2026/sort_the_odd.js` | **O(n log n)** | Sorting dominates asymptotic runtime. |
| `21-01-2026/your_oder_please.js` | **O(n²)** | Nested iteration or repeated linear lookup inside a loop. |
| `21-02-2026/freestyle_skiing.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `21-02-2026/freestyle_skiing.py` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `21-02-2026/ski_jumping.js` | **O(n log n)** | Sorting dominates asymptotic runtime. |
| `21-02-2026/ski_jumping.py` | **O(n log n)** | Sorting dominates asymptotic runtime. |
| `23-02-2026/biathlon.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `23-02-2026/biathlon.py` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `23-02-2026/groundhog_day.js` | **O(1)** | Only constant-time arithmetic/branching detected. |
| `23-02-2026/groundhog_day.py` | **O(1)** | Only constant-time arithmetic/branching detected. |
| `23-02-2026/snowboarding.js` | **O(1)** | Only constant-time arithmetic/branching detected. |
| `23-02-2026/snowboarding.py` | **O(1)** | Only constant-time arithmetic/branching detected. |
| `23-02-2026/string_mirror.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `23-02-2026/string_mirror.py` | **O(1)** | Only constant-time arithmetic/branching detected. |
| `24-02-2026/pocket_change.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `24-02-2026/pocket_change.py` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `24-02-2026/truncate_text.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `24-02-2026/truncate_text.py` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `26-01-2026/anagram_detection.js` | **O(n log n)** | Sorting dominates asymptotic runtime. |
| `26-01-2026/arithmetic_function.js` | **O(1)** | Only constant-time arithmetic/branching detected. |
| `26-01-2026/next_multiple_of_5.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `26-01-2026/string_increment.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `27-01-2026/break_camelcase.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `27-01-2026/count_char_string.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `27-01-2026/jaden_casing_string.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `27-01-2026/list_filtering.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `28-01-2026/extract_domain_URL.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `28-01-2026/find_the_odd_int.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `28-01-2026/highest_profit_wins.js` | **O(n log n)** | Sorting dominates asymptotic runtime. |
| `28-01-2026/two_sum.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `28-02-2026/blood_type_compatibility.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `28-02-2026/blood_type_compatibility.py` | **O(1)** | Only constant-time arithmetic/branching detected. |
| `28-02-2026/letter_and_number_count.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `28-02-2026/letter_and_number_count.py` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `28-02-2026/sequential_difference.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `28-02-2026/sequential_difference.py` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `29-01-2026/build_tower.js` | **O(n²)** | Nested iteration or repeated linear lookup inside a loop. |
| `29-01-2026/delete_occurrences.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `29-01-2026/highest_scoring_word.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `29-01-2026/playing_with_digits.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `30-01-2026/are_they_the_same.js` | **O(n log n)** | Sorting dominates asymptotic runtime. |
| `30-01-2026/counting_duplicates.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `30-01-2026/detect_pangram.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `30-01-2026/replace_with_alphabet_position.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `30-01-2026/who_likes_it.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `31-01-2026/array_helpers.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `31-01-2026/duplicate_encoder.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `31-01-2026/fibonacci_tribonacci_friends.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `31-01-2026/number_expanded_form.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `31-01-2026/tribonacci_sequence.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |
| `31-01-2026/unique_in_order.js` | **O(n)** | Single-pass iteration or linear built-in operations on input. |

> Notes: Static analysis can miss data-dependent behavior; estimates are intended as a practical quick-reference, not formal proofs.
